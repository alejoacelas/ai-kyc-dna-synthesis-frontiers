Figure S3: Average number of web searches per customer by model configuration (40-profile subset). Web searches are summed across both prompts (main screening and background work) per customer, then averaged per model. Green bars indicate all-tools configurations; blue bars indicate web-only configurations. Models with specialized tool access consistently perform fewer web searches.