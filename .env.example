# Required only for scripts/generate_datasets.py (latency fetching and country classification).
# Not needed for figure generation — figures read from processed/ CSVs.
# Use --skip-latency and --skip-country flags as alternatives.
OPENROUTER_API_KEY=sk-or-v1-your-key-here
